{
    "name": "discordbot",
    "version": "1.0.0",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "dev": "nodemon --ext ts,js -r tsconfig-paths/register src/bot.ts",
        "build": "tsc --project tsconfig.json && tsc-alias -p tsconfig.json",
        "start": "node dist/discordbot/src/bot.js"
    },
    "dependencies": {
        "discord.js": "^14.22.1"
        
    }
}
